# Copy to `.dev.vars` (gitignored) for local `npm run dev`.
# In production these are set encrypted via `wrangler secret put`, never in a file.

ANTHROPIC_API_KEY=sk-ant-...
APP_TOKEN=pick-a-long-random-string
